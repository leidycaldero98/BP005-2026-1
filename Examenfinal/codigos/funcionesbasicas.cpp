#include <iostream>
#include <cmath>

using namespace std;

int main() {

    cout << sqrt(16.0) << endl;   // Raíz cuadrada
    cout << pow(2.0, 3.0) << endl; // Potencia
    cout << fabs(-3.5) << endl;    // Valor absoluto

    return 0;
}
