#include <iostream> // Incluye la librería para imprimir.

using namespace std; // Permite usar cout y endl directamente.

int main() { // Inicia la función principal.

    int resultado1 = 2 + 3 * 4; // Primero calcula 3 * 4 y luego suma 2.
    int resultado2 = (2 + 3) * 4; // Primero calcula 2 + 3 por los paréntesis.

    cout << resultado1 << endl; // Muestra el primer resultado.
    cout << resultado2 << endl; // Muestra el segundo resultado.

    return 0; // Finaliza el programa correctamente.
}
